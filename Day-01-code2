#include <stdio.h>

int main() {
    int a,b;
    printf("enter the value of first number:");
    scanf("%d\n", &a);
    printf("enter the value of second number:");
    scanf("%d\n", &b);
    printf("sum= %d\n",a+b);
    printf("diffrence= %d\n", a-b);
    printf("product=%d\n", a*b);
    if (b!=0)
    printf("quotient=%d\n",a/b);
    else
    printf("quotient= the value doesnt exist");

    return 0;

}

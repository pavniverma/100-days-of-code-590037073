#include <stdio.h>

int main() {
    int num;
    scanf("%d", &num);
    if(num>=0)
    {
        if(num==0)
          printf(" The num is Zero");
        else
          printf("the num is positive");

    }
    else{
        printf("the num is negative");
    }
    return 0;

}
